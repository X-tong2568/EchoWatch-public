# EchoWatch（留声）开发者指南：如何监测 B站 UP主 评论区互动

> **适用读者**：想理解或复刻「B站 UP主 评论互动实时监测 + 邮件通知」的开发者。
>
> 本指南不重复 README 的运维内容，专门讲 **「怎么做」和「为什么这么做」**——每个 API 怎么调、WBI 签名怎么算、评论区怎么轮询、置顶评论为什么容易漏。

---

## 目录

1. [整体架构概览](#1-整体架构概览)
2. [如何拿到 UP主 的动态列表](#2-如何拿到-up主-的动态列表)
3. [如何拿到评论区](#3-如何拿到评论区)
4. [如何判断 UP主 互动了](#4-如何判断-up主-互动了)
5. [WBI 签名 —— 为什么不依赖 Cookie](#5-wbi-签名--为什么不依赖-cookie)
6. [分级轮询策略](#6-分级轮询策略)
7. [Priority 高频轮询](#7-priority-高频轮询)
8. [置顶评论的处理（top_replies）](#8-置顶评论的处理top_replies)
9. [代理与风控](#9-代理与风控)
10. [邮件通知](#10-邮件通知)
    - [10.4 富媒体渲染 —— 表情+评论图片](#104-富媒体渲染--表情评论图片)
    - [10.5 随机主题系统](#105-随机主题系统)

---

## 1. 整体架构概览

```
┌──────────────────────────────────────────────────────────┐
│                     main.py (入口)                         │
│  ┌───────────────────────────────────────────────────┐   │
│  │              scheduler.py (调度中心)                │   │
│  │                                                   │   │
│  │  ┌───────────────────┐  ┌───────────────────┐    │   │
│  │  │ 场景一 (自身作品)   │  │ 场景二 (话题互动)   │    │   │
│  │  │                   │  │                   │    │   │
│  │  │ discover → poll   │  │ discover → poll   │    │   │
│  │  │  300s      300s   │  │  300s     300s    │    │   │
│  │  │         +         │  │         +         │    │   │
│  │  │   screenshotter   │  │   screenshotter   │    │   │
│  │  │   (入库时截图)     │  │   (入库时截图)     │    │   │
│  │  └────────┬──────────┘  └────────┬──────────┘    │   │
│  │  ┌────────┴──────────────────────┴────────────┐  │   │
│  │  │ 场景三 (切片视频评论) ── discover → poll    │  │   │
│  │  │  多切片员 space feed → 评论匹配 target_uid  │  │   │
│  │  │  截图暂缓：标记 pending 由补截循环分批补齐   │  │   │
│  │  └────────────────────┬───────────────────────┘  │   │
│  │                       │                          │   │
│  │  ┌────────┴──────────────────────┴────────────┐  │   │
│  │  │   pinned_dynamic_monitor.py                │  │   │
│  │  │   置顶自动发现 (每小时) + 变更检测 + config回写 │  │   │
│  │  └────────┬──────────────────────────────────┘  │   │
│  │           │                                      │   │
│  │  ┌────────┴──────────────────────────────────┐  │   │
│  │  │     priority 独立高速通道 (随机 1~5s)       │  │   │
│  │  └────────────────────┬──────────────────────┘  │   │
│  │                       │                          │   │
│  │           ┌───────────┴───────────┐              │   │
│  │           │    bili_client.py     │              │   │
│  │           │  aiohttp + WBI 签名   │              │   │
│  │           └───────────┬───────────┘              │   │
│  │                       │                          │   │
│  │           ┌───────────┴───────────┐              │   │
│  │           │     database.py       │              │   │
│  │           │   SQLite (aiosqlite)  │              │   │
│  │           └───────────┬───────────┘              │   │
│  │                       │                          │   │
│  │           ┌───────────┴───────────┐              │   │
│  │           │   email_notifier.py   │              │   │
│  │           │   即时通知 + 日报汇总   │              │   │
│  │           │   富媒体渲染 + 随机主题 │              │   │
│  │           └───────────────────────┘              │   │
│  └───────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

**三个场景，各自独立的轮询池**：

| 场景 | 监测对象 | UP是谁 | `up_action.like` | 轮询通道 |
|------|---------|--------|:---:|---------|
| scene1 | 目标UP主自身作品 | 目标UP主 | ✅ 追踪 | L1+L2+Priority |
| scene2 | 话题粉丝帖子 | 发帖粉丝 | ❌ 不追踪 | L1+L2 |
| scene3 | 切片员投稿评论区 | 切片员（作品归属） | ❌ 不追踪 | L1+L2 |

> 场景三匹配的是配置的 `target_uid`（目标UP主），不是切片员；互动记录 `up_uid` = 目标UP主，
> 监测项 `up_uid` = 切片员。三场景的监测池完全隔离（`get_items_by_level(source=)`），不会交叉污染。

**六条独立的轮询通道**：

| 通道 | 间隔 | 范围 | 锁 | 说明 |
|------|------|------|:--:|------|
| Priority | 1~5s 随机 | 1 条置顶 | 无 | 不参与降级 |
| L1(场景一) | 300s | 0~24h | ✅ | 热点期 |
| L2(场景一) | 900s | 24~120h | ✅ | 温期 |
| L1(场景二) | 300s | 0~24h | ✅ | 热点期 |
| L2(场景二) | 900s | 24~120h | ✅ | 温期 |
| L1(场景三) | 300s | 0~24h | ✅ | 热点期 |
| L2(场景三) | 900s | 24~120h | ✅ | 温期 |

> 场景三额外有：发现循环（600s）、子评论基线扫查（600s）、批量通知（180s，合并一封）。
> 首次发现的旧切片（pub_ts 超 24h）自动落 L2/L0，只轮询 24h 内的新切片。

轮询方式：串行逐个，上一轮未完成则跳过（`_polling_l1/_polling_l2` 布尔锁）。
每 5 分钟独立遍历活跃项重新分级（`recheck_all_levels`），不依赖轮询触发。

---

## 2. 如何拿到 UP主 的动态列表

### 2.1 API 端点（双重策略）

```python
# bili_client.py 第24-25行
SPACE_FEED_URL = "https://api.bilibili.com/x/polymer/web-dynamic/v1/feed/space"

# 降级端点（空间动态不可用时）
# bili_client.py 第368行
"https://api.bilibili.com/x/space/wbi/arc/search"  # WBI 视频搜索
```

**策略**：
1. 先调 `polymer/web-dynamic/v1/feed/space`（新版空间动态 API，需要 buvid3 + WBI 签名）
2. 失败则降级到 `x/space/wbi/arc/search`（WBI 视频搜索，仅覆盖视频作品）
3. topic 动态走 `polymer/web-dynamic/v1/feed/topic`

### 2.2 请求方式

```python
# bili_client.py 第291-300行 —— WBI 签名 GET
async def _signed_get(self, url: str, params: dict) -> dict:
    mixin = await self._fetch_mixin_key()
    signed = wbi_sign_params(params, mixin)
    session = await self._get_session()
    async with session.get(url, params=signed) as resp:
        data = await resp.json()
    if data.get("code") != 0:
        raise RuntimeError(f"API错误 url={url} code={data.get('code')}")
    return data.get("data", {})
```

**关键点**：
- **零 Cookie**：不传登录态，全靠 WBI 签名 + buvid3（匿名设备指纹）
- buvid3 只需首次访问 B站首页自动获取（`_ensure_buvid`），之后复用
- WBI 密钥从 `nav` 接口获取，缓存 1 小时
- `trust_env=True` 读取 `HTTPS_PROXY` 环境变量，配合服务器代理绕过风控

### 2.3 响应结构解析

```python
# bili_client.py 第136-174行 —— _parse_space_feed
items = data.get("items", [])
for item in items:
    dyn_id = item.get("id_str", "")          # 动态ID
    dyn_type = item.get("type", "")          # 类型（AV/DRAW/WORD/FORWARD等）
    basic = item.get("basic", {})
    comment_oid = str(basic.get("comment_id_str", ""))

    modules = item.get("modules", {})
    mod_dyn = modules.get("module_dynamic", {})

    # 视频类型优先用 aid（视频评论区），而非动态自身的 comment_id_str
    major = mod_dyn.get("major") or {}
    archive = major.get("archive") or {}
    aid = str(archive.get("aid", ""))
    if dyn_type == "DYNAMIC_TYPE_AV" and aid:
        comment_oid = aid  # ← 视频的评论区 oid

    if not comment_oid:
        comment_oid = dyn_id  # 最后兜底
```

**为什么 AV 类型特殊处理**：视频类动态（DYNAMIC_TYPE_AV）的 `comment_id_str` 指向动态自己的评论线程（转发贴的评论），而真正的视频评论区 oid 是 `aid`。UP主通常评论的是视频本身，不是转发贴。

### 2.4 新动态检测

```python
# database.py 第155-199行 —— upsert_item
# INSERT OR IGNORE，item_id 为主键
# 返回 True = 新插入，False = 已存在
```

**简单有力**：同一个 `item_id`（B站动态ID）只入库一次。发现轮询每次拉取最新动态列表，新 ID 插入，旧 ID 跳过。

### 2.5 归档机制

```python
# database.py 第174-182行
# first_seen_at 用内容的发布时间（pub_ts），按真实年龄自然归档
if pub_ts_int > 0:
    first_seen = datetime.fromtimestamp(pub_ts_int).strftime(...)
else:
    first_seen = datetime.now().strftime(...)  # 无pub_ts时用当前时间
```

**为什么用发布时间**：B站 API 返回的 `pub_ts` 是内容真实发布时间。用发布时间做 `first_seen_at`，内容按自然年龄走 L1(0~24h) → L2(24~120h) → 归档(>120h)，符合设计预期。Priority 项（`is_priority=1`）不受此影响，永远保持 L1 高频轮询。

---

## 3. 如何拿到评论区

### 3.1 API 端点（双层：一级评论 + 子评论）

```python
# bili_client.py 第22-23行
MAIN_COMMENT_URL = "https://api.bilibili.com/x/v2/reply/wbi/main"  # 一级评论
SUB_COMMENT_URL = "https://api.bilibili.com/x/v2/reply/reply"      # 子评论(楼中楼)
```

### 3.2 一级评论 —— 游标翻页

```python
# bili_client.py 第386-415行 —— get_comments
params = {"oid": oid, "type": comment_type, "mode": 2}  # mode=2 按时间排序
if pagination_str and pagination_str != "0":
    params["pagination_str"] = pagination_str  # 游标翻页

data = await self._signed_get(MAIN_COMMENT_URL, params)
return {
    "replies": data.get("replies") or [],
    "cursor": data.get("cursor", {}),
    "top_replies": data.get("top_replies") or [],  # ← 置顶评论！
}
```

**翻页控制**（`monitor_scene1.py` 第242-250行）：
```python
cursor = resp.get("cursor", {})
if cursor.get("is_end"):
    break                                    # 优先检查 is_end
pag_reply = cursor.get("pagination_reply", {})
pagination_str = pag_reply.get("next_offset", "")
if not pagination_str or pagination_str == "no-next-offset":
    break
```

**为什么先检查 is_end**：部分动态类型的 `pagination_reply` 为空对象 `{}`（不是缺少字段），此时 `.get("next_offset", "")` 返回空字符串但翻页不应继续。`is_end` 是更可靠的终止信号。

**确定性错误快速失败（v1.4.0）**：`get_comments` 遇到 `-400`（翻页游标无效/参数错误）、`-404`（无评论）、`12002`（评论功能已关闭）时**不重试**（这些错误重试无意义，且 async_retry 的 50s×3=150s 阻塞会卡死 priority 每 1~5s 的轮询循环），直接返回空结果 + `disabled: True` 标记。调用方（`_poll_item`）遇 `disabled` 直接结束本轮。

**必须走干净会话（v1.8.0 修复，重要）**：`get_comments` 内部不再用主会话 `_signed_get`，而是
手动 WBI 签名后走独立干净会话 `_get_sub_session()`（与子评论接口同一方案）。
**原因（2026-08-19 实测）**：主会话调过 space feed/topic feed（种下 buvid）后，本接口
可见评论条数被 B站 降级（实测 9 条 → 3 条，且只返回时间最新的子集）——含子评论的
关键根评论会从可见列表消失 → 子评论基线建不起来 → 目标回复漏检。A/B 对照：
主会话 space feed 后 3 条（关键楼消失），干净会话恒 9 条（含关键楼）。
> 排查"评论可见条数异常少"时先检查会话 cookie 状态；评论类接口（main/reply）一律走干净会话，
> 主会话只用于 space feed/topic feed/nav 等需要 buvid 的接口。

### 3.3 子评论 —— 翻页与节流

```python
# monitor_scene1.py —— _process_sub_comments
MAX_SUB_PAGES = 10  # v1.4.0: 5→10（200条），覆盖大楼评论
for page in range(1, MAX_SUB_PAGES + 1):
    resp = await self.client.get_sub_comments(
        oid=oid, comment_type=comment_type,
        root_rpid=root_rpid, page_index=page,
    )
    # ...
    page_info = resp.get("page", {})
    total = page_info.get("count", 0)
    if page * 20 >= total:
        break
```

**翻页行为（v1.4.0 实测）**：子评论 API（`x/v2/reply/reply`）的 `pn` 参数翻页**正常**（pn=1~5+ 均返回数据），早期"pn>1 返回空"的限制已不复现，可完整翻取大楼评论（如 107 条）。

**检测节流（v1.4.0 新增）**：为避免每轮对同一根评论反复翻页打爆 API，子评论检测受基线节流控制：
- 基线存 `sub_comment_baseline` 表（每根评论一行，只存当前值，翻页后 upsert 覆盖）
- 触发条件：基线无记录（首次见）→ 立即翻 + 写基线；有记录 → **rcount 变大 且 距上次翻 ≥ 2min** 才翻
- 翻页成功才更新基线（失败等下一轮重试，不吞新回复）

### 3.4 评论类型映射

```python
# bili_client.py 第37-44行
DYNAMIC_TYPE_TO_COMMENT = {
    "DYNAMIC_TYPE_AV": 1,           # 视频
    "DYNAMIC_TYPE_DRAW": 11,        # 图文动态
    "DYNAMIC_TYPE_ARTICLE": 12,     # 专栏
    "DYNAMIC_TYPE_WORD": 17,        # 纯文字动态
    "DYNAMIC_TYPE_FORWARD": 17,     # 转发动态 → 按文字动态
    "DYNAMIC_TYPE_LIVE_RCMD": 1,    # 直播回放 → 按视频
}
```

**为什么 FORWARD 映射到 17**：转发动态的评论区行为和纯文字动态一致，B站 API 要求的 `type` 参数也是 17。

---

## 4. 如何判断 UP主 互动了

### 4.1 两个检测条件

```python
# monitor_scene1.py 第237-293行 —— _process_comment
parsed = parse_comment(raw)
rpid = parsed["rpid"]      # 评论ID（B站唯一）
mid = parsed["mid"]        # 评论者UID
up_liked = parsed["up_action_like"]  # UP觉得很赞

# 条件1：UP主 自己的评论
if mid == up.uid:
    await self.db.insert_interaction({...})

# 条件2：UP觉得很赞（且不重复 UP主 自己的评论）
if up_liked and mid != up.uid:
    await self.db.insert_interaction({...})

# 条件3：子评论（楼中楼）
if replies_count > 0:
    await self._process_sub_comments(...)  # 递归检测
```

### 4.2 去重与通知

```python
# database.py 第269-294行 —— insert_interaction
# INSERT OR IGNORE，comment_id 为主键
# 返回 True = 新互动，False = 已存在（不重复通知）

# 通知流程：
# interactions 表 → notified_immediate = 0 → 定时器每30s扫描 → 发邮件 → 标记已通知
```

**为什么用 INSERT OR IGNORE 而不是先查后插**：原子操作，无竞态条件。评论 ID（rpid）在 B站 全局唯一，天然适合做主键。

### 4.3 子评论上下文重建

```python
# monitor_scene1.py 第336-342行 —— 被回复内容查找
# 构建子评论查找表（rpid → {uname, content}）
sub_lookup = {}
for raw in all_subs:
    parsed = parse_sub_comment(raw)
    sub_lookup[parsed["rpid"]] = {"uname": ..., "content": ...}

# 对每条 UP主 子回复，查找被回复者
if parent_rpid == root_rpid:
    parent_content = root_context["content"]       # 回复根评论
elif parent_rpid in sub_lookup:
    parent_content = sub_lookup[parent_rpid]["content"]  # 回复其他子评论
else:
    parent_content = ""  # 上下文未知
```

**为什么需要上下文**：邮件通知里要展示「UP主 回复了 X：...」，没有上下文只看 UP主 的话会不知所云。

---

## 5. WBI 签名 —— 为什么不依赖 Cookie

### 5.1 签名算法

```python
# bili_client.py 第69-104行
# 1. 从 nav 接口获取 img_url 和 sub_url
# 2. 提取文件名并拼接
img_key = img_url.split("/")[-1].split(".")[0]
sub_key = sub_url.split("/")[-1].split(".")[0]
origin = img_key + sub_key

# 3. 按固定混淆表重排取前32位
MIXIN_TABLE = [46,47,18,2,53,...]  # 64个位置，B站前端硬编码
mixin_key = "".join(origin[n] for n in MIXIN_TABLE)[:32]

# 4. 参数排序 + URL编码 + 拼mixinKey → MD5
params_with_ts = {**params, "wts": int(time.time())}
query = urllib.parse.urlencode(sorted(params_with_ts.items()))
w_rid = hashlib.md5((query + mixin_key).encode()).hexdigest()
```

### 5.2 为什么零 Cookie 可行

| 接口 | 需要什么 | 说明 |
|------|---------|------|
| `nav` | 无 | 未登录也返回 `wbi_img`（code=-101 仍然有数据） |
| `reply/wbi/main` | WBI 签名 | 公开接口，签名即可 |
| `reply/reply` | **必须无 cookie** | 子评论不需要签名；且带 buvid 会被静默截断（见 5.3） |
| `polymer/feed/space` | WBI + buvid3 | 空间动态需要设备指纹 |
| `polymer/detail` | WBI + buvid3 | 动态详情同样需要 |

**核心发现**：B站公开 API 只需要 WBI 签名 + buvid3（匿名设备 ID），不需要登录态。buvid3 通过访问一次首页自动获取。
与登录态无关——SESSDATA/bili_jct 全程不用，没有账号风控/封号风险，也没有 Cookie 过期维护。

### 5.3 子评论专用独立会话（v1.6.0，重要）

**实现**：`BiliClient._get_sub_session()` 为子评论请求创建独立会话——独立 CookieJar，**永不种 buvid**，与主会话完全隔离。主会话仍负责种 buvid，供 space feed / topic feed 等需要设备指纹的接口使用。

**为什么不共用主会话**：会话一旦带上有效匿名指纹（buvid3+b_nut），B站对 `reply/reply` 按「匿名受限」策略处理——从第 2 页起**静默返回空列表**（code=0 不报错）。而楼中楼按时间升序排列、UP 回复靠后，翻页永远只拿到第 1 页 10 条，靠后的 UP 回复会**全部漏检**。完全无指纹反而走 legacy 路径全量返回（Freeview 插件用 `credentials:'omit'` 所以免疫）。

### 5.4 与 BTCE 的区别

| | BTCE | EchoWatch |
|------|------|------|
| 方式 | Playwright 浏览器 + Cookie | aiohttp HTTP + WBI 签名 |
| 需要 Cookie | 是（SESSDATA 约7天过期） | 否 |
| 资源消耗 | 200-500MB（Chromium） | 极低（纯 HTTP） |
| 检测速度 | 受页面加载影响（5-15s） | API 调用（<1s） |
| 适用场景 | 置顶评论截图 + 图片变更 | 评论内容文本检测 |

---

## 6. 分级轮询策略

### 6.1 四级模型

```
新发现 → L1 (热点, 300s) → L2 (温期, 900s) → L0 (归档, 停止)
              0h                24h                 120h
Priority: 独立通道 (1~5s)，不受分级影响，始终 L1
```

| 级别 | 年龄（按 `pub_ts` 发布时间） | 轮询间隔 | 说明 |
|------|---------------------------|---------|------|
| Priority | 不受限 | 1~5s 随机 | `is_priority=1`，永远 L1 |
| L1 热点 | 0 ~ 24h | 300s | 高频，有锁 |
| L2 温点 | 24h ~ 120h | 900s | 低频，有锁 |
| L0 归档 | > 120h | 停止 | `monitor_level=0`，不再轮询 |

> **L0（归档）= 不再监测**：`monitor_level=0` 的 item 不会出现在任何轮询查询里（`get_items_by_level` 只查 L1/L2），监测完全停止。归档是终态——即使内容之后又出现新互动也不会恢复监测（`_check_level_transition` 对已归档项直接返回 0），除非 item 重新入库。

> **`first_seen_at` = 帖子真实的 `pub_ts`（发布时间），不是发现时间。** 这是归档准确的保证。

```python
# monitor_scene1.py —— _check_level_transition
def _check_level_transition(self, item: dict) -> int:
    if item.get("is_priority"):
        return 1                      # Priority 永远 L1

    if current_level == 0:
        return 0                      # 已归档不再恢复

    age_hours = (datetime.now() - seen_dt).total_seconds() / 3600

    if current_level == 1 and age_hours > t.level1_hours:  # >24h
        return 2
    if current_level == 2 and age_hours > t.level2_hours:  # >120h
        return 0                      # 归档
    return current_level
```

### 6.2 定期重新分级 + 锁机制

**分级触发方式（双保险）**：
1. **轮询时触发**：每次 `_poll_item` 调用前执行 `_check_level_transition`
2. **定时遍历触发**：`recheck_all_levels()` 每 5 分钟遍历所有活跃项，独立重新分级

> 方式 2 确保 L2 项即使没被轮询也能正常降级到 L0，不依赖轮询频率。

**轮询锁**：
```python
if self._polling_l1:
    return  # 上一轮未完成，跳过
self._polling_l1 = True
try:
    for item in items:
        await asyncio.sleep(random(1, 3))
        await self._poll_item(item, up)
finally:
    self._polling_l1 = False
```
串行逐个轮询，上一轮没跑完就不启动下一轮，防止多轮重叠打爆 B站 API。

### 6.3 轮询顺序：新内容优先

```python
SELECT * FROM monitored_items WHERE monitor_level = ? AND source = ?
ORDER BY first_seen_at DESC  -- 最新发布的先轮询
```

**为什么新内容优先**：新发布的动态更可能有 UP主 互动。让最新的先跑。

### 6.4 场景二预测窗口

**实现**：每个话题维护一个「预测窗口」——检测到 UP主 在话题里互动后记录互动位置，轮询时把互动位置之前的最新 20 条帖子（`PREDICTION_WINDOW_SIZE`）纳入 L1 高频轮询（即使超过 24h 也保持）；距上次互动超过 `prediction_idle_minutes`（默认 120 分钟）自动清空窗口，帖子回归常规分级。

**为什么需要**：UP主 在话题里的互动往往连续发生——在某帖互动后，附近的最新帖子更可能成为下一次互动对象。把这些帖子保持高频轮询，能更快捕获新互动；长时间无互动后清空窗口，避免帖子永久占用高频轮询资源。

---

## 7. Priority 高频轮询

### 7.1 独立高速通道（随机 1~5s）

```python
# scheduler.py —— 随机间隔防风控
async def _scene1_priority_loop(self):
    while self.running:
        for up in self.config.up_list:
            await self.scene1.poll_priority_only(up)
        await asyncio.sleep(random.uniform(1, 5))  # 随机1~5s
```

```python
# monitor_scene1.py —— 无请求间延迟
async def poll_priority_only(self, up: UpConfig):
    items = await self.db.get_priority_items()
    for item in items:
        await self._poll_item(item, up)  # 没有 random delay
```

**与普通 L1 轮询的区别**：
- 轮询间隔随机 1~5s（非固定），防风控
- 请求间无额外延迟，全速
- 独立 asyncio 任务，不阻塞 L1/L2
- 始终只有一条（config 切换时自动清理旧标记）

### 7.2 自动清理旧标记

```python
# database.py 第230-237行
async def clear_stale_priority(self, keep_ids: list[str]):
    """清除不在 keep_ids 中的 priority 标记"""
    await self._conn.execute(
        f"UPDATE monitored_items SET is_priority = 0
         WHERE is_priority = 1 AND item_id NOT IN ({placeholders})",
        keep_ids,
    )
```

**为什么需要自动清理**：config 里改了 `priority_dynamics` 后，旧的 priority 项如果不清理标记，会和新的一起享受 5s 高频轮询。priority 的价值在于「集中火力监测那一条最重要的动态」，多了就稀释了。

### 7.3 为什么比 BTCE 快

```
BTCE: Playwright 打开页面 → 等渲染 → 抓DOM → 比文本 → 推送 (~5-15s)
EchoWatch: HTTP API → 解析JSON → 比mid → 推送 (<1s)
```

而且 EchoWatch 在评论刚发出时就能检测到（在 `replies` 里），不需要等 UP主 设为置顶（在 `top_replies` 里）。

---

## 8. 置顶评论的处理（top_replies）

### 8.1 API 返回结构

B站评论 API 的 `data` 包含**两个独立的评论列表**：

```json
{
  "data": {
    "replies": [...],        // 普通评论（按时间排序）
    "top_replies": [...]      // 置顶评论（独立字段）
  }
}
```

### 8.2 处理方式

**为什么必须单独处理**：置顶评论只在 `top_replies` 字段里返回，不会出现在 `replies` 里。只遍历 `replies` 的话，UP主 把评论设为置顶后就完全检测不到。

```python
# monitor_scene1.py 第238-243行
replies = resp.get("replies", [])
top_replies = resp.get("top_replies", [])

# 置顶评论单独处理（仅第一页有）
for raw in top_replies:
    await self._process_comment(raw, oid, comment_type, item_id, up)

for raw in replies:
    await self._process_comment(raw, oid, comment_type, item_id, up)
```

### 8.3 置顶评论 vs 普通评论的时序

UP主 的操作流程通常是：
1. 发评论 → 进入 `replies` → EchoWatch **秒级检测到**
2. 设为置顶 → 进入 `top_replies` → BTCE 此时才检测到

所以 EchoWatch 理论上比 BTCE 更快——评论发出就能捕获，不用等设为置顶。

---

## 9. 代理与风控

### 9.1 为什么需要代理

服务器 IP（腾讯云中国）频繁被 B站 风控（`-352 风控校验失败`、`-412 request was banned`）。通过代理轮换出口 IP，绕过单 IP 限制。

### 9.2 代理方案

```
echowatch (aiohttp)
    │ trust_env=True → 读取 HTTPS_PROXY
    ▼
127.0.0.1:7890 (Mihomo/Clash Meta)
    │ 规则：只有 B站 域名走代理
    ▼
SS/Trojan 节点 → B站 API
```

### 9.3 节点存活保障

- **健康检查**：每 120s 检测节点可用性，死节点自动切换
- **定时重启**：cron 每 1 小时 `systemctl restart mihomo`，强制刷新订阅 + 节点全量轮换
- **协议选择**：只用 TCP 友好的协议（SS、Trojan），避免 hysteria2（Python asyncio SSL 不兼容）

### 9.4 hysteria2 与 Python SSL 的兼容性问题

**现象**：`ssl:default [None]` / `ConnectionResetError`

**原因**：hysteria2 是 UDP-based 协议，与 Python asyncio 的 `start_tls` 机制不兼容。curl（基于 OpenSSL/libcurl）能正常通过，但 aiohttp 不行。

**结论**：必须用 TCP-based 协议（SS / Trojan / VLESS）。

---

## 10. 邮件通知

### 10.1 两套通知体系

| 类型 | 间隔 | 触发条件 |
|------|------|---------|
| 即时通知 | 每 30s 扫描 | 有新互动且 `notified_immediate = 0`（场景一主评论） |
| 批量通知 | 每 180s | 场景二/三互动合并一封（`send_scene2_batch` / `send_scene3_batch`） |
| 日报汇总 | 每天 22:00 | 当日所有互动汇总 |

### 10.2 邮件内容

```python
# email_notifier.py
# - 场景标签（自身作品 / 话题互动 / 切片视频）
# - UP主 评论内容 + 上下文（回复了谁、被回复者说了什么）
# - 评论直达链接（#reply{rpid}，支持子评论锚点）
# - 随机主题配色（HSL 全色相）
# - 场景二/三：原帖上下文卡片（截图优先，文本+视频降级）
# - 不渲染 B站 自动搜索词链接（jump_url，v1.8.0 起清洗）
```

### 10.3 为什么用 QQ邮箱 SMTP

QQ邮箱 SMTP SSL（端口 465）在国内服务器上最稳定，不会像海外 SMTP（Gmail）那样被间歇性阻断。需要 QQ邮箱设置中的「授权码」而非登录密码。

### 10.4 富媒体渲染 —— 表情+评论图片

#### 数据来源

B站评论 API 返回的 `content` 对象不仅包含 `message`（纯文本），还包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `emote` | object | `{"[表情名]": {"url": "https://...", "meta": {"size": 1|2}}}` |
| `pictures` | array | `[{"img_src": "https://...", "img_width": 400, ...}]` |
| `jump_url` | object | `{"原始URL": {"title": "...", "pc_url": "..."}}`（B站自动搜索词链接，v1.8.0 起**不渲染**） |

v1.0.0 新增 `parse_comment()` 和 `parse_sub_comment()` 返回的 `rich_content` 字段，将上述三者序列化为 JSON 字符串。

#### 渲染函数

```python
def render_comment_html(content_text: str, rich_content_str: str = None) -> str
```

处理流程：
1. **降级**：rich_content 为空 → `html.escape` 纯文本，兼容旧数据
2. **表情替换**：遍历 `emote`，将文本中 `[表情名]` 替换为 `<img class="emote-small|large" src="https://...">`
3. **换行保留**：`\n` → `<br>`
4. **评论图片**：遍历 `pictures[]`，去 `@` 水印后缀，放入 `.images-container`（flex-wrap 容器）
5. **超链接**：遍历 `jump_url`，替换为可点击 `<a>` 标签

#### CSS 样式

```css
.emote-small { width: 22px; height: 22px; vertical-align: middle; }
.emote-large { width: 44px; height: 44px; vertical-align: middle; }
.images-container { display: flex; flex-wrap: wrap; gap: 8px; }
.comment-image { max-width: 280px; max-height: 280px; object-fit: contain; }
```

#### 数据库迁移

`interactions` 表新增 `rich_content TEXT` 列。`database._migrate_add_rich_content()` 用 `PRAGMA table_info` 检测列是否存在，不存在则 `ALTER TABLE ADD COLUMN`。旧数据 rich_content 为 NULL，渲染时自动降级为纯文本。

### 10.5 随机主题系统

#### 设计思路

借鉴 BTCE3.0 `email_renderer.py` 的随机渐变配色思路（`color_config.py` 60+对对比色），并在此基础上扩展了随机维度——不仅配色随机，**纹理、阴影、分割线全部随机**，每封邮件视觉上截然不同。

| 维度 | BTCE3.0 做法 | EchoWatch 做法 |
|------|-------------|---------------|
| 配色 | 60+对预设对比色随机选取 | 色相 0-360 完全随机 HSL |
| 纹理 | 无 | 4种纯CSS背景图案叠加header |
| 阴影 | 固定 | 3档随机(轻/中/重) |
| 分割线 | 固定虚线 | 4种随机(渐变/圆点/双线/虚线) |

#### 4种纹理

全部用纯 CSS 背景实现，无需图片资源：

1. **星河** — 圆点星 + 十字星芒（`ellipse` 横竖交叠模拟☆光芒），呼应"小星星"粉丝名
2. **月辉** — 双弦月（白圆 + 偏移暗圆切割出月牙形状）+ 伴星
3. **流光** — 3颗流星统一 ↗ 方向，金/白/灰三色尾迹延伸到顶部
4. **涟漪** — 四层细环白暗交替，模拟立体水波浮雕

#### 预览模式

```bash
python email_notifier.py --preview
```

生成 6 封不同主题测试邮件到 `sent_emails/`（已在 .gitignore 排除），文件名含纹理名+色值便于对比，浏览器直接打开查看。

#### 随机主题函数

```python
def random_theme() -> dict:
    return {
        "primary":       "#3a8fd4",     # 主色
        "secondary":     "#d47f3a",     # 副色
        "texture":       "radial-gradient(...)",  # header纹理CSS
        "texture_name":  "星河",         # 纹理名
        "shadow":        "0 2px 12px rgba(0,0,0,0.10)",  # 卡片阴影
        "divider":       "border: 0; height: 2px; background: linear-gradient(...)",  # 分割线
        "radius":        10,             # 固定圆角
    }
```

#### footer 渐变分割线

使用 `<div class="footer-divider">` 元素 + `background: linear-gradient(90deg, 主色, 副色)` 实现渐变分割线（`border-image` 邮件客户端兼容性差，弃用）。

#### 纹理 CSS 实现代码

以下为 `_TEXTURES` 中 4 种纹理的完整 CSS，叠加在 header 渐变之上（多重 background）：

**星河** — 圆点星 + 十字星芒

```css
background:
    /* 主星1：左侧，带十字光芒 */
    radial-gradient(circle at 12% 18%, rgba(255,255,255,0.30) 3px, transparent 3px),
    radial-gradient(ellipse 16px 3px at 12% 18%, rgba(255,255,255,0.20) 0%, transparent 100%),
    radial-gradient(ellipse 3px 16px at 12% 18%, rgba(255,255,255,0.20) 0%, transparent 100%),
    /* 主星2：中部 */
    radial-gradient(circle at 48% 58%, rgba(255,255,255,0.24) 3.5px, transparent 3.5px),
    radial-gradient(ellipse 18px 3.5px at 48% 58%, rgba(255,255,255,0.17) 0%, transparent 100%),
    radial-gradient(ellipse 3.5px 18px at 48% 58%, rgba(255,255,255,0.17) 0%, transparent 100%),
    /* 主星3：右侧 */
    radial-gradient(circle at 78% 16%, rgba(255,255,255,0.28) 3px, transparent 3px),
    radial-gradient(ellipse 16px 3px at 78% 16%, rgba(255,255,255,0.19) 0%, transparent 100%),
    radial-gradient(ellipse 3px 16px at 78% 16%, rgba(255,255,255,0.19) 0%, transparent 100%),
    /* 主星4：中下 */
    radial-gradient(circle at 25% 72%, rgba(255,255,255,0.22) 3.5px, transparent 3.5px),
    radial-gradient(ellipse 17px 3.5px at 25% 72%, rgba(255,255,255,0.18) 0%, transparent 100%),
    radial-gradient(ellipse 3.5px 17px at 25% 72%, rgba(255,255,255,0.18) 0%, transparent 100%),
    /* 辅星：无光芒圆点 */
    radial-gradient(circle at 88% 62%, rgba(255,255,255,0.20) 2.5px, transparent 2.5px),
    radial-gradient(circle at 58% 35%, rgba(255,255,255,0.18) 2.5px, transparent 2.5px),
    radial-gradient(circle at 20% 42%, rgba(255,255,255,0.16) 2px, transparent 2px),
    radial-gradient(circle at 68% 80%, rgba(255,255,255,0.17) 2px, transparent 2px),
    /* 主题渐变底色 */
    linear-gradient(135deg, primary, secondary);
```

**月辉** — 双弦月 + 伴星

```css
background:
    /* 大弦月：白圆 + 偏移暗圆切割出月牙 */
    radial-gradient(circle at 78% 16%, rgba(255,255,255,0.26) 0%, transparent 24px),
    radial-gradient(circle at 73% 22%, rgba(0,0,0,0.12) 0%, transparent 18px),
    radial-gradient(circle at 82% 10%, rgba(255,255,255,0.32) 0%, transparent 8px),
    /* 小弦月：左下角 */
    radial-gradient(circle at 30% 62%, rgba(255,255,255,0.22) 0%, transparent 16px),
    radial-gradient(circle at 26% 56%, rgba(0,0,0,0.10) 0%, transparent 12px),
    radial-gradient(circle at 34% 58%, rgba(255,255,255,0.26) 0%, transparent 5px),
    /* 伴星 */
    radial-gradient(circle at 8% 40%, rgba(255,255,255,0.16) 2.5px, transparent 2.5px),
    radial-gradient(circle at 52% 28%, rgba(255,255,255,0.15) 2px, transparent 2px),
    radial-gradient(circle at 55% 70%, rgba(255,255,255,0.14) 2px, transparent 2px),
    radial-gradient(circle at 18% 75%, rgba(255,255,255,0.15) 1.5px, transparent 1.5px),
    radial-gradient(circle at 88% 55%, rgba(255,255,255,0.14) 2px, transparent 2px),
    radial-gradient(circle at 92% 50%, rgba(255,255,255,0.13) 1.5px, transparent 1.5px),
    /* 主题渐变底色 */
    linear-gradient(135deg, primary, secondary);
```

**流光** — 3颗流星统一↗方向，金/白/灰三色尾迹

```css
background:
    /* 流星1：左侧，金色尾迹 */
    radial-gradient(circle at 18% 72%, rgba(255,255,255,0.32) 2.5px, transparent 3px),
    linear-gradient(42deg, transparent 52%, rgba(255,215,0,0.02) 68%, 
                   rgba(255,215,0,0.10) 71%, rgba(255,215,0,0.02) 72%, transparent 73%),
    /* 流星2：中部，白色尾迹 */
    radial-gradient(circle at 50% 55%, rgba(255,255,255,0.30) 2px, transparent 2.5px),
    linear-gradient(42deg, transparent 35%, rgba(255,255,255,0.02) 50%, 
                   rgba(255,255,255,0.12) 54%, rgba(255,255,255,0.02) 55%, transparent 56%),
    /* 流星3：右侧，银灰尾迹 */
    radial-gradient(circle at 82% 68%, rgba(255,255,255,0.28) 2px, transparent 2.5px),
    linear-gradient(42deg, transparent 48%, rgba(200,200,220,0.02) 63%, 
                   rgba(200,200,220,0.08) 67%, rgba(200,200,220,0.01) 68%, transparent 69%),
    /* 主题渐变底色 */
    linear-gradient(135deg, primary, secondary);
```

> **流星尾迹原理**：`linear-gradient` 横跨整个 header（~70px高），在流星头位置(如72%)突然增亮为可见拖尾，其余区域保持透明。流星头用 `radial-gradient` 圆点锚定视觉焦点。

**涟漪** — 四层细环，白暗交替，模拟立体浮雕

```css
background:
    /* 第1层：白实心中心 */
    radial-gradient(circle at 48% 36%, rgba(255,255,255,0.22) 0%, transparent 18%),
    /* 第2层：暗环(凹感)，环宽1.5px */
    radial-gradient(circle at 48% 36%, transparent 32%, rgba(0,0,0,0.07) 34%, 
                   rgba(0,0,0,0.07) 35.5%, transparent 37%),
    /* 第3层：白环(凸感)，环宽1.5px */
    radial-gradient(circle at 48% 36%, transparent 52%, rgba(255,255,255,0.14) 54%, 
                   rgba(255,255,255,0.14) 55.5%, transparent 57%),
    /* 第4层：暗环(凹感)，环宽1.5px */
    radial-gradient(circle at 48% 36%, transparent 70%, rgba(0,0,0,0.05) 72%, 
                   rgba(0,0,0,0.05) 73.5%, transparent 75%),
    /* 主题渐变底色 */
    linear-gradient(135deg, primary, secondary);
```

> **立体感原理**：白环模拟凸起受光面，暗环模拟凹陷阴影面。环宽仅 1.5px，四层交替形成浮雕般的水波涟漪效果。

#### 阴影与分割线

```python
_SHADOWS = [
    "0 1px 6px rgba(0,0,0,0.06)",    # 轻盈
    "0 2px 12px rgba(0,0,0,0.10)",   # 标准
    "0 4px 20px rgba(0,0,0,0.12)",   # 厚重
]

_DIVIDER_TEMPLATES = [
    "border: 0; height: 2px; background: linear-gradient(90deg, {p}, {s});",
    "border: 0; border-top: 2px dotted #ccc;",
    "border: 0; border-top: 1px solid #e0e0e0; border-bottom: 1px solid #f0f0f0; height: 3px;",
    "border: 0; border-top: 1px dashed #d0d0d0;",
]
```

---

## 附录：关键 API 速查表

| 用途 | API 端点 | 签名 | 需要 Cookie |
|------|---------|:---:|:---:|
| 获取 WBI 密钥 | `api.bilibili.com/x/web-interface/nav` | 无 | 否 |
| 空间动态列表 | `api.bilibili.com/x/polymer/web-dynamic/v1/feed/space` | WBI | buvid3 |
| 动态详情 | `api.bilibili.com/x/polymer/web-dynamic/v1/detail` | WBI | buvid3 |
| 话题动态列表 | `api.bilibili.com/x/polymer/web-dynamic/v1/feed/topic` | WBI | buvid3 |
| 一级评论 | `api.bilibili.com/x/v2/reply/wbi/main` | WBI | 否 |
| 子评论 | `api.bilibili.com/x/v2/reply/reply` | 无 | 否 |
| 视频搜索（降级） | `api.bilibili.com/x/space/wbi/arc/search` | WBI | 否 |
| 用户公开信息 | `api.bilibili.com/x/web-interface/card` | 无 | 否 |
| 首页（获取buvid3） | `www.bilibili.com/` | 无 | 否 |

---

> **最后更新**：2026年7月31日
>
> 本文档基于 EchoWatch v1.3.1 代码库编写。涵盖 v1.0.1 基础架构至 v1.3.1 最新功能（置顶动态自动发现、元素级截图、邮件缩放等）。与本项目配套的另一份参考是 BTCE 的 [开发者指南](../BTCE3.0/开发者指南-仿站必读.md)，侧重浏览器抓取 + Playwright 方案，本文档侧重纯 API + WBI 签名方案，互为补充。
